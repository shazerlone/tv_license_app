// Native implementation — no-op for now (onboarding always shows in dev anyway).
// Later: swap for shared_preferences to persist on device.
bool hasSeenOnboarding() => false;

void markOnboardingSeen() {}
