# Desktop copy-trading app (prototype) dependencies
customtkinter==5.2.2
psutil==5.9.8

# Build tooling for producing the Windows .exe
pyinstaller==6.6.0

# Live MT5 trading — official API. Windows-only, so the marker keeps Linux/Mac
# dev installs working while the Windows .exe build picks it up.
MetaTrader5==5.0.4682 ; sys_platform == "win32"
