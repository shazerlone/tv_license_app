# Desktop copy-trading app (prototype) dependencies
customtkinter==5.2.2
psutil==5.9.8

# Build tooling for producing the Windows .exe
pyinstaller==6.6.0

# --- Live trading (Windows only, add when wiring real connectors) ---
# MetaTrader5==5.0.45   # official MT5 Python API (no MT4 equivalent)
